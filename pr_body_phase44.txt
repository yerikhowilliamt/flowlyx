## Summary
This Pull Request implements Phase 44 (Production Deployment).

## Files Changed
- `apps/api/Dockerfile`: Created a multi-stage, production-ready Dockerfile using `turbo prune` for minimal image size and running as a non-root user.
- `.dockerignore`: Added standard ignores to minimize Docker build context.
- `infrastructure/docker-compose.prod.yml`: Added the production orchestration configuration for API, PostgreSQL, and Redis.

## Testing Evidence
- ESLint and TypeScript compilation passed successfully.
- Docker build verified locally without errors.
- Checked configuration against Flowlyx Engineering Handbook standards.

## Breaking Changes
None. This is an additive infrastructure configuration.

## Migration Notes
- When deploying, ensure `infrastructure/docker-compose.prod.yml` is used (`docker-compose -f infrastructure/docker-compose.prod.yml up -d`).

## Rollback Plan
- Revert this commit if the deployment orchestration conflicts with the existing environment.

## Reviewer Checklist
- [ ] Architecture follows Handbook (multi-stage builds, non-root user).
- [ ] Proper environment variables are exposed in `docker-compose.prod.yml`.
- [ ] `node_modules` and other heavy folders are excluded via `.dockerignore`.
